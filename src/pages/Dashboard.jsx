import React,{useEffect,useState} from 'react'; import {api} from '../api'; import {CalendarDays,Clock3,Trophy,ClipboardList,ArrowRight} from 'lucide-react'; import {Link} from 'react-router-dom'; import {useAuth} from '../App'; import {getDateRange,rangeOptions} from '../dateRanges'; import AdminAnalytics from '../components/AdminAnalytics'; import StaffAnalytics from '../components/StaffAnalytics'; import InterviewCalendar from '../components/InterviewCalendar';
export default function Dashboard(){const {user}=useAuth();return user.role==="admin"?<AdminAnalytics/>:user.role==='staff'?<StaffAnalytics user={user}/>:<StandardDashboard/>}

function StandardDashboard(){const {user}=useAuth(),[stats,setStats]=useState(null),[recent,setRecent]=useState([]),[period,setPeriod]=useState('all'),[loading,setLoading]=useState(false);useEffect(()=>{const range=getDateRange(period),params=new URLSearchParams(Object.entries(range).filter(([,v])=>v));setLoading(true);Promise.all([api('/interviews/stats?'+params),api('/interviews?limit=5&'+params)]).then(([s,r])=>{setStats(s);setRecent(r.items)}).finally(()=>setLoading(false))},[period]);const cards=[['Total interviews',stats?.total??'—',ClipboardList,'purple'],['Scheduled',stats?.scheduled??'—',CalendarDays,'blue'],['In progress',stats?.inProgress??'—',Clock3,'amber'],['Selected / Placed',stats?.selected??'—',Trophy,'green']];const periodLabel=rangeOptions.find(([key])=>key===period)?.[1];return <><div className="welcome"><div><span className="eyebrow">{new Date().toLocaleDateString('en-IN',{weekday:'long',day:'numeric',month:'long'})}</span><h1>Good {new Date().getHours()<12?'morning':new Date().getHours()<17?'afternoon':'evening'}, {user.name.split(' ')[0]}.</h1><p>Here’s what’s happening with your interview pipeline.</p></div><Link className="primary" to="/interviews/new">+ Schedule interview</Link></div><div className="dashboard-period"><div><strong>Dashboard period</strong><span>Showing {periodLabel?.toLowerCase()}</span></div><div className="quickranges compactranges">{rangeOptions.map(([key,label])=><button key={key} className={period===key?'active':''} onClick={()=>setPeriod(key)}>{label}</button>)}</div></div><div className={loading?'statgrid loadingstats':'statgrid'}>{cards.map(([label,value,Icon,color])=><article className="stat" key={label}><span className={`icon ${color}`}><Icon/></span><div><strong>{value}</strong><p>{label}</p></div></article>)}</div><div className="user-overview-grid"><div className="dashgrid"><section className="panel"><div className="panelhead"><div><h3>Recent interviews</h3><p>Latest records from {periodLabel?.toLowerCase()}</p></div><Link to="/interviews">View all <ArrowRight size={16}/></Link></div><div className="recent">{recent.length?recent.map(x=><div className="recentrow" key={x._id}><div className="avatar soft">{x.candidateName.slice(0,2).toUpperCase()}</div><div className="grow"><strong>{x.candidateName}</strong><small>{x.technology} · {x.companyName}</small></div><div className="date"><strong>{new Date(x.interviewDate).toLocaleDateString('en-IN',{day:'2-digit',month:'short'})}</strong><small>{x.interviewTime}</small></div><span className={`status ${x.status.toLowerCase().replace(' ','-')}`}>{x.status}</span></div>):<div className="empty">No interviews in this period.</div>}</div></section><section className="panel"><div className="panelhead"><div><h3>Technology mix</h3><p>Top categories for {periodLabel?.toLowerCase()}</p></div></div><div className="bars">{stats?.technology?.length?stats.technology.map(t=><div key={t._id}><div><span>{t._id}</span><strong>{t.count}</strong></div><i><b style={{width:`${Math.max(12,t.count/(stats.total||1)*100)}%`}}/></i></div>):<div className="empty">No analytics for this period.</div>}</div></section></div><InterviewCalendar role={user.role}/></div></>}
