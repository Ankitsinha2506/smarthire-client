import React from 'react';
import {CalendarClock} from 'lucide-react';

export default function TomorrowSchedule({items=[],showSupport=false}){
  return <section className="panel tomorrow-panel"><div className="panelhead"><div><span className="eyebrow">NEXT UP</span><h3>Tomorrow’s schedule</h3><p>{items.length} interview{items.length===1?'':'s'} planned for tomorrow</p></div><span className="tomorrow-icon"><CalendarClock/></span></div><div className="tomorrow-list">{items.length?items.map(item=><article key={item._id}><time>{item.interviewTime||'Time pending'}</time><div className="avatar soft">{item.candidateName.slice(0,2).toUpperCase()}</div><div className="tomorrow-candidate"><strong>{item.candidateName}</strong><small>{item.candidateEmail||'Email not available'}</small></div><div className="tomorrow-detail"><strong>{item.technology||'Technology not specified'}</strong><small>{item.companyName||'Company not specified'}</small></div><div className="tomorrow-contact"><strong>{item.hrName||'HR not specified'}</strong><small>{item.hrMobile||item.hrEmail||'Contact not available'}</small></div>{showSupport&&<div className="tomorrow-support"><small>Support</small><strong>{item.assignedStaff?.map(person=>person.name).join(', ')||'Unassigned'}</strong></div>}<span className={`status ${item.status.toLowerCase().replace(' ','-')}`}>{item.status}</span></article>):<div className="tomorrow-empty"><CalendarClock/><strong>No interviews tomorrow</strong><span>Newly scheduled interviews will appear here.</span></div>}</div></section>
}
